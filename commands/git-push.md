---
description: Git push - safely push the CURRENT branch to its configured upstream; if the remote is ahead, reconcile with rebase or merge and retry without silently overwriting remote history. Usage: /git-push [--rebase|--merge] [--dry-run] [--no-verify] [--force-with-lease]
---

Load the git-push skill and follow it strictly.

User request: $ARGUMENTS

---
description: Git pull - sync the CURRENT branch with its configured upstream. Tries `git pull --ff-only` first; if diverged, guard-backs up and delegates to git-merge (local branch is ours) or, with `--rebase`, replays local commits onto the remote baseline. Usage: /git-pull [--rebase] [--dry-run] [--no-verify] [--abort]
---

Load the git-pull skill and follow it strictly.

User request: $ARGUMENTS

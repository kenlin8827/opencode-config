---
description: Git merge - sync target to origin first (--ff-only), then merge a source branch into it; the agent replaces the human at conflict resolution (semantic, hunk-by-hunk, target HEAD is the authoritative baseline), then continues the merge. Usage: /git-merge <source> <target> [--dry-run|--no-verify|--squash|--rebase|--no-ff|--abort]
agent: build
---

Load the git-merge skill and follow it strictly.

User request: $ARGUMENTS
---
description: Review-Dev - mission-critical dual-review consensus loop: domain-routed coding + dual review + Advisor arbitration (default max 10 rounds). Usage: /review-dev <task> [--max-rounds=N]
agent: build
---

Load the dev skill and execute it with the **review-dev preset**: `--code-review=2` (--max-rounds default 10).

User request: $ARGUMENTS

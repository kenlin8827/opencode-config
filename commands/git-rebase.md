---
description: Git rebase - replay ALL of a source branch's unique commits onto a target branch's HEAD for a clean linear history; the agent replaces the human at per-commit conflict resolution (semantic, hunk-by-hunk, the onto-branch HEAD is the authoritative baseline), then fast-forwards target. Rewrites source history (both tips guard-backed-up; force-with-lease is the user's call). Usage: /git-rebase <source> <target> [--dry-run|--no-verify|--continue|--skip|--abort]
---

Load the git-rebase skill and follow it strictly.

User request: $ARGUMENTS
